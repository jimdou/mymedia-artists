<%= form_for(artist) do |f| %>
  <% if artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :genre %>
    <%= f.text_field :genre %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>

  <div class="field">
    <%= f.label :birthDate %>
    <%= f.datetime_select :birthDate %>
  </div>

  <div class="field">
    <%= f.label :deathDate %>
    <%= f.datetime_select :deathDate %>
  </div>

  <div class="field">
    <%= f.label :activeFrom %>
    <%= f.datetime_select :activeFrom %>
  </div>

  <div class="field">
    <%= f.label :activeUntil %>
    <%= f.datetime_select :activeUntil %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
